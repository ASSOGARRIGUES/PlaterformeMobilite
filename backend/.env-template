SECRET_KEY=
DEBUG=True
PROD=False

URL_CSRF = https://*.rezal.fr
BASE_URL =

DB_TYPE =
DB_ADDR =
DB_USERNAME=
DB_PASSWORD=
DB_NAME=

EMAIL_HOST=
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=
SERVER_EMAIL=mail.rezal.fr

# Sentry
SENTRY_DSN=                 # DSN from Sentry Settings → Client Keys (used by the tunnel)
VITE_SENTRY_DSN=            # same value — passed as build arg to the frontend Docker image
SENTRY_AUTH_TOKEN=          # auth token for source map upload at build time only
